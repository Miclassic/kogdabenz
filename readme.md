# ⛽ КогдаБенз — «когда лучше заправиться?»

Система временнóго прогнозирования для водителей юга России (Краснодарский край и Адыгея).
Мы не копируем чужие карты наличия топлива — мы отвечаем на вопрос «КОГДА»:
когда на конкретной АЗС вероятно вернётся топливо и когда лучше заехать.
Цель: собрать историю изменений АЗС → найти закономерности → предсказать окно
пополнения → проверять прогнозы на фактах → улучшать точность.

## Как работает система

```
GdeBenz API (домашняя рамка: Новороссийск+Геленджик+Анапа+Крымск, ~137 АЗС)
        + донор-рамка "Кубань+Адыгея" (~698 АЗС, region='kuban')
          ↑ каждые 10 минут будит cron-job.org → workflow_dispatch
 collector.js (GitHub Actions, чистый fetch, v5):
    дом: станции → наблюдения → события (топливо/очереди, gap-детектор, фильтр противофазы)
    донор: станции → экономная запись (смена статуса / heartbeat 1ч / первая встреча)
    → народные отметки user_feedback → события → уборка наблюдений >30 дней
    → Telegram-лента «живая хроника» (с учётом тихого режима bot_meta.notify)
          ↓
 Supabase: stations / observations / events / predictions /
           user_feedback / bot_meta + вьюхи (security_invoker)
          ↓
 predictor.js v1.5:
    медиана±σ по Москве, пулинг own→brand→city, модель длительности дефицита,
    k-NN по эпизодам "исчезло→вернулось" (порог ≥8, иначе медиана-фолбэк),
    features-снапшот (час/день, возраст дефицита, очередь+тренд+queue_age_min,
    регион, соседи без 95, возвраты/исчезновения, режим, k-NN),
    режим региона (NORMAL/LOCAL/CITY/RECOVERY/UNKNOWN), гейт молчащих станций,
    baseline_restore_at для сравнения, прицел target_date "сегодня"/"завтра"
          ↓
 verifier.js v1.2 (02:10 MSK, ежедневно):
    оценивает вчерашние прогнозы по фактам → SUCCESS/MISS → is_verified
    + величина ошибки: actual_restore_at, error_minutes, baseline_error_minutes
    + TRAINING DATA в логе (MAE/медиана/p90 + bias + разбивки по режиму/источнику/k-NN/часу)
          ↓
 prediction_training (вьюха): верифицированные прогнозы с features и ошибкой
          ↓ (по мере накопления ≥100 строк)
 калибровка confidence по децилям · тюнинг k-NN · shadow-сравнение версий
          ↓
 digest.js (08:00 MSK, ежедневно): сводка за 24 ч в Telegram
 bot.js (каждые 5 мин): читает обновления Telegram, отвечает на кнопки /status /predict /digest /quiet /loud
          ↓
 Сайт (GitHub Pages): карточки + карта спроса (геолокация и точка «я здесь»,
    кластеры/пины/зоны с пульсом, палитра пинов по топливу и градиентные пины,
    бейдж очереди, таблички названий, вылетающие чипы, кольцо действий,
    быстрая отметка с подтверждением) + тепловая матрица + пульс региона
    + метрики (покрытие источника, возраст очереди, цены города в карточке)
    + панель «Наша точность прогнозов» + «Почему такой прогноз»
    + «Куда ехать» с гейтом свежести и расстоянием + фильтр/сортировка
    + тумблер «город пользователя / весь юг» на кардиограмме
    + карточка «Помощь» простым языком + приветственное окно первого захода
```

## Состав репозитория

| Файл | Роль | Статус |
| --- | --- | --- |
| collector.js | Сборщик v5: дом + доноры, экономная запись доноров (heartbeat 1ч + смены), лукбэк 24ч, живая лента, gap-детектор, фильтр противофазы, народные отметки, уборка | ✅ прод |
| predictor.js | Предиктор v1.5: пулинг own/brand/city, модель дефицита + k-NN, снапшот features (включая queue_age_min), режим региона (включая UNKNOWN ночью), гейт молчащих станций, baseline_restore_at | ✅ прод |
| verifier.js | Верификатор v1.2: величина ошибки + TRAINING DATA с bias и разбивками; logTrainingData вынесена в функцию и вызывается на обоих путях | ✅ прод |
| probe.js | Разведка покрытия GdeBenz по рамкам (без записи в базу): станции / с топливом / с ценами / с очередями | ✅ прод |
| digest.js | Утренний дайджест за 24 ч | ✅ прод |
| bot.js | Бот с кнопками: отчёты и тихий режим (серверлесс, без сервера) | ✅ прод |
| index.html | Сайт v2.7: карточки + карта спроса + регион (Краснодарский край + Адыгея) + геолокация и точка «я здесь» + тумблер «город пользователя / весь юг» на кардиограмме + динамическая линейка процентов + карточка «Помощь» + приветственное окно первого захода + «Куда ехать» с гейтом свежести и расстоянием + тапабельные чипы «что это?» с объяснениями | ✅ прод |
| .github/workflows/collect.yml | dispatch + «Telegram on failure» | ✅ прод |
| .github/workflows/digest.yml | dispatch дайджеста | ✅ прод |
| .github/workflows/verify.yml | dispatch верификатора | ✅ прод |
| .github/workflows/bot.yml | dispatch бота (каждые 5 мин) | ✅ прод |
| .github/workflows/probe.yml | dispatch разведки покрытия (по требованию) | ✅ прод |
| README.md | Этот документ | ✅ |

Внешние сервисы:
- cron-job.org — основной будильник: каждые 10 минут — `collect`, каждые 5 минут — `bot`, ежедневно 08:00 MSK — `digest`, ежедневно 02:10 MSK — `verify`. Все через GitHub API `workflow_dispatch`. PAT fine-grained: Actions Read and write, только этот репозиторий.
- Telegram-бот «КогдаБенз Алерт» — живая хроника событий, 🔮 первые прогнозы, 🚨 аварии, 🌅 дайджест, кнопки-отчёты, тихий режим.
- Родной GitHub `schedule` на этом репозитории не срабатывал ни разу — внешний будильник считается основным.

## База данных (Supabase)

| Таблица | Что хранит |
| --- | --- |
| stations | Реестр АЗС (external_id = osm_id, brand, address, lat/lon, source, region, fuels_meta) |
| observations | Снимки статусов каждые 10 мин (+ data_freshness_minutes, queue_level, цены); для доноров — экономно (смены/heartbeat 1ч) |
| events | Изменения: топливные, очередные, народные |
| predictions | Окна прогнозов + prediction_source + based_on_stations + expected_restore_at + baseline_restore_at + target_date + result/is_verified + features (jsonb-снапшот признаков) + model_version + actual_restore_at + error_minutes + baseline_error_minutes |
| user_feedback | Структурированные народные отметки: feedback_type + fuel_type + queue_size + comment_text |
| bot_meta | Состояние бота (update_offset, notify режим) — только service_role |

Вьюхи агрегатов (обход лимита 1000 строк + безопасный RLS):
- `station_counts`, `station_hourly`, `station_last_price` — по станции
- `city_*` — домашний срез (bbox Новороссийска)
- `region_hourly`, `region_fuel_hourly`, `region_events`, `region_daily_events` — разрез по городам юга + `south` (весь регион): питают тепловую матрицу, кардиограмму, чипы «Сейчас/Пик/Спокойно», плашку карты и теглайн
- `city_queue_episodes` — эпизоды очереди gaps-and-islands
- `prediction_training` — учебная выборка

Все вьюхи установлены с `security_invoker = true`.

## Что сделано (статус на 19.09.2026)

- [x] Этапы 1–6: API GdeBenz, коллектор на Actions, история наблюдений, gap-детектор + фильтр противофазы, предиктор v1→v1.1, верификатор v1, сайт v2, пульс, бот v2
- [x] Тепловая матрица + кардиограмма День/Неделя/Месяц + мультивыбор + дайджест-панель + защищённая загрузка + честные пустые состояния + чипы «Сейчас/Пик/Спокойно» (16.09)
- [x] Обучающий контур v1.2 + верификатор v1.2 (величина ошибки) + предиктор v1.3 (k-NN) + блок «Почему такой прогноз» + collector v4 без комментариев (17.09)
- [x] Предиктор v1.4 (гейт молчащих > 7 сут) + selling-нормировка очередей + «Куда ехать» с гейтом свежести + baseline + TRAINING DATA + фильтр/сортировка + живая лента + режим UNKNOWN ночью (ночь 17→18.09)
- [x] Волна 1: донор Кубань+Адыгея (~698 станций), stations.region, экономная запись, вьюхи city_events/city_daily_events с bbox (ночь 17→18.09)
- [x] Карта спроса MapLibre + OpenFreeMap positron + зоны/кластеры/пины/тап-полёт + подписи городов (18.09)
- [x] Геолокация и точка «я здесь» + зоны v2 (дышащие кольца) + палитра пинов по топливу + градиентные пины + таблички названий + вылетающие чипы + кольцо действий + пузырь быстрой отметки с подтверждением + «Показать на карте» (ночь 18→19.09)
- [x] Метрика ① цены города (вьюхи + ряд в карточке) + ② покрытие источника (чип со спарклайном + раскрываемый список с полётом) + ③ возраст очереди (вьюха эпизодов + медиана в дайджесте) + queue_age_min в features (предиктор v1.5) (ночь 18→19.09)
- [x] Панель «Наша точность прогнозов» слева от главной с честным нулевым состоянием и эволюцией 3+/10+/100+ + стабильность кластеров при панорамировании + гуманизация текстов + «Куда ехать» с расстоянием (штраф min(25, км × 1.5)) (19.09)
- [x] Тапабельные чипы «что это?» с объяснениями и полезной добавкой для водителей (чипы главной панели + плашка карты + ячейки карточки) + тултип кардиограммы с переворотом вниз у верхних точек (19.09)
- [x] **Расширение на весь юг (19.09):**
  - Сайт: список «АЗС города» показывает весь юг (~835 станций); сортировка по расстоянию от геолокации
  - Региональные вьюхи `region_hourly` / `region_fuel_hourly` / `region_events` / `region_daily_events` с разрезом по 9 городам + `south`
  - Кардиограмма: тумблер «город пользователя / весь юг» (по умолчанию — город по геолокации)
  - Тепловая матрица, чипы «Сейчас/Пик/Спокойно», плашка карты, теглайн, дайджест-панель — следуют за тумблером
  - `detectUserCity()` вызывается на старте сайта и после «Рядом» — тумблер и подпись кардиограммы подстраиваются под город пользователя
  - Линейка процентов слева на кардиограмме с динамическим максимумом по текущим данным
- [x] **Карточка «Помощь» и приветственное окно (19.09):**
  - Кнопка «Помощь» рядом с «Рядом» через разделитель «|»
  - Шторка с 8 вопросами-ответами простым языком без технических терминов
  - Приветственное окно при первом заходе (один раз на устройство, галочка в localStorage `seenHello`); кнопки «Понятно, смотрю» и «Расскажите подробнее» (открывает полную карточку)
- [x] **Collector v5 (19.09):** heartbeat доноров 1 час вместо 6 ч — почасовые сводки региона живые
- [x] **Predictor: весь юг (19.09):** `OWN_BOX` расширена на всю рамку дом+донор (43.20–46.10 / 37.20–41.60), прогнозы и очереди считаются по всем станциям; поштучный запрос `events` для исчезновений убран в пользу загруженного массива `lastDisByPair`

## Правила проекта (обязательны для любого нового кода)

- Запросы к GdeBenz — только с `BROWSER_HEADERS` и повторами.
- Рабочий код не переписываем с нуля: возможности дописываются шагами.
- Новые скрипты — один файл, чистый fetch, без npm-зависимостей.
- Всё время — явно по Москве (UTC+3).
- Факт и прогноз в интерфейсе разделены всегда.
- Точность показываем только при ≥10 проверенных прогнозах.
- Прогноз прицеливается на дату `target_date`.
- Новый тип события → сначала белый список constraint, потом код.
- Вьюхи — всегда `security_invoker = true`.
- Народные отметки → в базе только значения из белого списка (RLS policy).
- Отказоустойчивость сайта: каждый внешний запрос к вьюхе в своём try/catch.
- Пустые состояния видимы: нет данных → серая плашка с объяснением.
- Обучение только на верифицированных исходах.
- `CREATE OR REPLACE VIEW`: новые колонки только дописываются в конец.
- Изменения модели — аддитивно с фолбэком на малых данных.
- Измерение раньше усложнения: сначала error_minutes/baseline_error_minutes, потом новые модели.
- Экономная запись донор-наблюдений: для `region` not null — только смены/heartbeat 1ч/первая встреча.
- Региональные сводки — из `region_*` вьюх с фильтром `city=<город>` или `city=south`.
- Советник «Куда ехать» учитывает расстояние: штраф min(25, км × 1.5); совет по открытому окну — только станциям с ненулевым статусом топлива.
- Геолокация определяет город пользователя (`detectUserCity()`); тумблер кардиограммы по умолчанию = город пользователя.

## Запуск и проверка

- Норма collect: `Станций в ответе: ~137` + `Донор kuban: станций ~698` + `Наблюдений записано: ~835 (дом 137 + доноры ~698)`; в последующих `доноры` = единицы-десятки (heartbeat 1ч) + блок предиктора с `Контекст региона: … режим …`, прогнозы по всем ~835 станциям
- Норма сайта: кардиограмма показывает город пользователя; тумблер «город / весь юг» переключает данные; тепловая матрица, чипы, плашка следуют за тумблером; «Куда ехать» ведёт на ближнюю станцию с топливом; тап по чипам открывает окошко «что это?»; первый заход показывает приветственное окно; кнопка «Помощь» открывает шторку с вопросами-ответами

## SQL-самопроверка

```sql
-- регионы
select region, count(*) from stations group by 1;
-- региональные сводки
select city, count(*) from region_hourly group by 1 order by 2 desc;
select city, count(*) from region_fuel_hourly group by 1 order by 2 desc;
-- прогнозы
select target_date, result, count(*) from predictions group by 1,2 order by 1 desc;
-- обучение
select count(*) filter (where error_minutes is not null) as с_ошибкой,
       count(*) filter (where baseline_error_minutes is not null) as с_baseline
from predictions where is_verified;
```

## Безопасность

- Секреты GitHub: `SUPABASE_URL`, `SUPABASE_KEY` (service_role), `TELEGRAM_BOT_TOKEN`, `TELEGRAM_CHAT_ID`.
- В клиентский код (`index.html`) — только anon-ключ.
- PAT для cron-job.org: fine-grained, Actions Read and write, один репозиторий.
- Антиспам народных отметок: ≤3 с устройства за 30 минут (localStorage) + RLS-белый список.
- Приветственное окно — один раз на устройство через `localStorage.seenHello`; сброс только через devtools.

## Хронология проекта

| Дата | Событие |
| --- | --- |
| 14.09.2026 | Первая версия сайта |
| 15.09.2026 | Коллектор, первый 🟢 fuel_restored, первые прогнозы, верификатор v1 |
| 16.09.2026 | Тепловая матрица + кардиограмма + мультивыбор + дайджест-панель |
| 17.09.2026 | Обучающий контур v1.2 + k-NN (v1.3) + блок «Почему такой прогноз» |
| 18.09.2026 | Карта спроса MapLibre; геолокация и точка «я здесь»; палитра пинов; кольцо действий; быстрая отметка; метрики ①②③ |
| 19.09.2026 | Панель точности + «Куда ехать» с расстоянием + тапабельные чипы «что это?» |
| 19.09.2026 вечер | **Расширение на весь юг**: сайт v2.7 с региональными вьюхами, тумблером «город / весь юг», динамической линейкой процентов, карточкой «Помощь», приветственным окном; коллектор v5 с heartbeat доноров 1ч; предиктор на весь юг |