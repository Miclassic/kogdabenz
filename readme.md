{
  "meta": {
    "name": "⛽ КогдаБенз — «когда лучше заправиться?»",
    "subtitle": "Система временнóго прогнозирования для водителей юга России",
    "home_base": "Домашняя база — Новороссийск; с волны 1 — весь Кубань+Адыгея; позиционирование — «КогдаБенз · юг»",
    "idea": "Мы не копируем чужие карты наличия топлива — мы отвечаем на вопрос «КОГДА»: когда на конкретной АЗС вероятно вернётся топливо и когда лучше заехать",
    "goal": "Собрать историю изменений АЗС → найти закономерности → предсказать окно пополнения → проверять прогнозы на фактах → улучшать точность",
    "status_date": "22.09.2026, поздно"
  },
  "architecture": [
    "GdeBenz API (домашняя рамка: Новороссийск+Геленджик+Анапа+Крымск, ~137 АЗС) + донор-рамка «Кубань+Адыгея» (~698 АЗС, region='kuban'); каждые 10 минут будит cron-job.org → workflow_dispatch",
    "collector.js (GitHub Actions, чистый fetch, v5.1): дом — станции → наблюдения → события (топливо/очереди, gap-детектор, фильтр противофазы); донор — экономная запись (смена статуса / heartbeat 1ч / первая встреча); народные отметки user_feedback → события; уборка наблюдений >30 дней; Telegram-лента «живая хроника» с учётом тихого режима bot_meta.notify; v5.1: 429/503/сеть = upstream-ошибка → мягкий выход 0 + троттлинг алерта ≤1 раза в час (bot_meta.upstream_alert) + джиттер старта ≤45 сек",
    "Supabase: stations / observations / events / predictions / user_feedback / site_hits / bot_meta + вьюхи (security_invoker)",
    "predictor.js v1.8: рамка ВСЕГО юга (дом + донор); медиана±σ по Москве; пулинг own→brand→city; модель длительности дефицита; k-NN по эпизодам «исчезло→вернулось» (порог ≥8, иначе медиана-фолбэк); v1.7: день недели в k-NN — круговое расстояние (0..3 дня) + класс будни/выходные; v1.8: калибровка уверенности по таблице bot_meta.calibration_table (нет таблицы / старше 21 дня / битая → сырая уверенность), conf_raw в features; features-снапшот (час/день, возраст дефицита, очередь+тренд+queue_age_min, регион, соседи без 95, возвраты/исчезновения, режим, k-NN, station_state); режим региона NORMAL/LOCAL/CITY/DEEP_SHORTAGE/RECOVERY/UNKNOWN; гейт молчащих станций; baseline_restore_at; прицел target_date «сегодня»/«завтра»",
    "verifier.js v1.2 (02:10 MSK, ежедневно): оценивает вчерашние прогнозы по фактам → SUCCESS/MISS → is_verified; величина ошибки: actual_restore_at, error_minutes, baseline_error_minutes; TRAINING DATA в логе (MAE/медиана/p90 + bias + разбивки по режиму/источнику/k-NN/часу); logTrainingData вызывается на обоих путях (пустой прогон тоже печатает)",
    "calibrator.js v3 (02:25 MSK, сразу после верификатора): свежесть — верифицированные строки за 14 дней (фолбэк 30 при нехватке); ворота: ≥30 строк И ≥10 SUCCESS И разброс бинов ≥15 п.п.; квантильные бины (≥5 строк, ≤8 бинов) + изотония PAVA; таблица → bot_meta.calibration_table (JSON-строка); ворота не пройдены → таблица САМОудаляется, предиктор на сырой уверенности",
    "prediction_training (вьюха): верифицированные прогнозы с features и ошибкой; измерение; shadow-сравнение версий по model_version — следующий шаг",
    "digest.js (08:00 MSK, ежедневно): сводка за 24 ч в Telegram",
    "bot.js (каждые 5 мин): читает обновления Telegram, отвечает на кнопки /status /predict /digest /quiet /loud",
    "Сайт (GitHub Pages, v2.9 + PWA): персонален по геолокации (список/чипы/плашка/кардиограмма = город пользователя, CITY_BOXES 9 городов); весь юг — карта и тумблер «город / весь юг»; region_* вьюхи с фильтром city; кардиограмма с динамической линейкой процентов; карта спроса (кластеры/пины/зоны с пульсом, градиентные пины, таблички, вылетающие чипы, кольцо действий, быстрая отметка с подтверждением); народный консенсус (2 голоса красят пин, 3 — «народ подтвердил», конфликт — оранжевый пин с «?», приоритет источника); прогноз очереди в карточке; тепловая матрица + пульс + метрики (покрытие, возраст очереди, цены); панель «Наша точность прогнозов» v2.9 (медианная ошибка → «в N раз точнее простого» → попадания счётом «X из Y» → проверено ночами; блок «улучшение модели · ошибка по неделям»); индикатор свежести данных v2.9 (строка в подзаголовке + чип плашки карты); «Почему такой прогноз»; «Куда ехать» с расстоянием; карточка «Помощь» + приветственное окно + кнопка «Попробовать снова»; минимальная аналитика site_hits v2.9; PWA: manifest + service worker (статика cache-first, API network-first, кэш только ok), установка на домашний экран; офлайн-режим: снимок kbCache + плашка «Нет связи — данные на ЧЧ:ММ» + отметки в fbQueue с автоотправкой"
  ],
  "repo": [
    { "file": "collector.js", "role": "Сборщик v5.1: дом + доноры, экономная запись доноров (heartbeat 1ч + смены), лукбэк 24ч, живая лента, gap-детектор, фильтр противофазы, народные отметки, уборка; v5.1 — upstream-отказоустойчивость (мягкий выход 0, троттлинг алерта ≤1ч, джиттер)", "status": "прод" },
    { "file": "predictor.js", "role": "Предиктор v1.8: рамка всего юга, пулинг own/brand/city, модель дефицита + k-NN (v1.7: день недели), v1.8: применение калибровки + conf_raw + model_version v1.2, снапшот features, режим региона, гейт молчащих, baseline_restore_at", "status": "прод" },
    { "file": "verifier.js", "role": "Верификатор v1.2: величина ошибки + TRAINING DATA с bias и разбивками; logTrainingData вынесена в функцию и вызывается на обоих путях", "status": "прод" },
    { "file": "calibrator.js", "role": "Калибратор уверенности v3: свежесть 14/30 дней, ворота ≥30 строк / ≥10 SUCCESS / разброс ≥15 п.п., бины + PAVA, таблица в bot_meta.calibration_table, самоудаление таблицы при провале ворот", "status": "прод" },
    { "file": "probe.js", "role": "Разведка покрытия GdeBenz по рамкам (без записи в базу): станции / с топливом / с ценами / с очередями", "status": "прод" },
    { "file": "digest.js", "role": "Утренний дайджест за 24 ч", "status": "прод" },
    { "file": "bot.js", "role": "Бот с кнопками: отчёты и тихий режим (серверлесс, без сервера)", "status": "прод" },
    { "file": "index.html", "role": "Сайт v2.9: всё из v2.8 + минимальная аналитика site_hits (анонимные события); переупаковка панели точности (медианная ошибка главным, «в N раз точнее», попадания счётом, строка измерено/ждут факта); индикатор свежести данных (порог 6ч, светофор, подзаголовок + чип плашки); блок «улучшение модели · ошибка по неделям»", "status": "прод" },
    { "file": "manifest.json", "role": "PWA-манифест: имя, тема, standalone, SVG-иконка (any maskable), относительные start_url / scope", "status": "прод" },
    { "file": "sw.js", "role": "Service worker: статика cache-first, API network-first, кэш только ok-ответов; CACHE_VERSION — механизм обновления (поднимать при любых правках index.html/manifest.json; в правках v2.9 поднимался на каждую заплатку)", "status": "прод" },
    { "file": ".github/workflows/collect.yml", "role": "dispatch + «Telegram on failure»", "status": "прод" },
    { "file": ".github/workflows/predict.yml", "role": "dispatch предиктора", "status": "прод" },
    { "file": ".github/workflows/digest.yml", "role": "dispatch дайджеста", "status": "прод" },
    { "file": ".github/workflows/verify.yml", "role": "dispatch верификатора", "status": "прод" },
    { "file": ".github/workflows/calibrate.yml", "role": "dispatch калибратора (native schedule как резерв, основной будильник — cron-job.org)", "status": "прод" },
    { "file": ".github/workflows/bot.yml", "role": "dispatch бота (каждые 5 мин)", "status": "прод" },
    { "file": ".github/workflows/probe.yml", "role": "dispatch разведки покрытия (по требованию)", "status": "прод" },
    { "file": "README.md", "role": "Документ проекта (этот JSON — его машинное представление)", "status": "актуален" },
    { "file": "statistics.js, confidence.js, handler.js, comments.js, events.js, gdebenz.js, scheduler.js, setup.js", "role": "Черновики-спецификации этапов E/F (confidence.js — легаси-дубль верификатора, не используется)", "status": "затем в legacy/" }
  ],
  "external_services": {
    "cron_job_org": "Основной будильник: каждые 10 минут — collect, каждые 5 минут — bot, ежедневно 06:00 MSK — predict, 02:10 MSK — verify, 02:25 MSK — calibrate, 08:00 MSK — digest; все через GitHub API workflow_dispatch; PAT fine-grained: Actions Read and write, только этот репозиторий",
    "telegram_bot": "«КогдаБенз Алерт» — живая хроника событий с человеческой формулировкой и выводом для водителя, 🔮 первые прогнозы, 🚨 аварии, дайджест, кнопки-отчёты, тихий режим",
    "github_schedule_note": "Родной GitHub schedule на этом репозитории не срабатывал ни разу — внешний будильник считается основным"
  },
  "database": {
    "tables": [
      { "name": "stations", "stores": "Реестр АЗС (external_id = osm_id, brand, address, lat/lon, source, region, fuels_meta)" },
      { "name": "observations", "stores": "Снимки статусов каждые 10 мин (+ data_freshness_minutes, queue_level, цены price_92/95/diesel); для доноров — экономно (смены/heartbeat 1ч)" },
      { "name": "events", "stores": "Изменения: топливные, очередные, народные" },
      { "name": "predictions", "stores": "Окна прогнозов + prediction_source + based_on_stations + expected_restore_at + baseline_restore_at + target_date + result/is_verified + features (jsonb-снапшот признаков, включая conf_raw) + model_version + actual_restore_at + error_minutes + baseline_error_minutes + verified_at" },
      { "name": "user_feedback", "stores": "Структурированные народные отметки: feedback_type + fuel_type + queue_size + comment_text + device_id" },
      { "name": "site_hits", "stores": "Анонимная аналитика сайта (v2.9): session_id, event, referrer, utm_source/medium/campaign, city_key, offline, panel; RLS — anon только insert, чтения наружу нет" },
      { "name": "bot_meta", "stores": "Состояние бота и сервиса (update_offset, notify) + upstream_alert (время последнего алерта о недоступности источника) + calibration_table (JSON-строка таблицы калибровки) — только service_role" },
      { "name": "comments", "stores": "Текстовые комментарии (источник не даёт — таблица спит; шаг парсинга убран в collector v4)" }
    ],
    "calibration_table_format": {
      "example": { "v": 2, "built_at": "2026-09-22T23:25:00.000Z", "window_days": 14, "n": 1967, "successes": 26, "points": [[0.37, 0.0], [0.41, 0.03], [0.46, 0.03]] },
      "note": "value — JSON-строка (переживает и jsonb, и text); points — монотонная (PAVA) таблица «сырая уверенность → фактическая доля попаданий»; предиктор интерполирует между точками и зажимает в 0.05–0.95"
    },
    "views": {
      "regional": [
        "region_hourly — почасовой пульс (marks, queue_marks, avail95, known95, selling) — кардиограмма «День» и теглайн",
        "region_fuel_hourly — 4 ряда тепла (АИ-92/95/ДТ/Очереди) + selling + knownany (покрытие источника) — тепловая матрица, чипы, плашка",
        "region_events — топливные события города/региона — колонны кардиограммы, дайджест-панель",
        "region_daily_events — дневные агрегаты исчезновений/возвратов — кардиограммы «Неделя/Месяц»"
      ],
      "home": [
        "station_counts, station_hourly — по станции",
        "city_hourly, city_fuel_hourly (+selling, +knownany), city_events, city_daily_events",
        "city_price_hourly — почасовые медианы цен по трём топливам + min/max АИ-95 + покрытие ценами",
        "station_last_price — последние известные цены станции (дельты «к городу» в карточке)",
        "city_queue_episodes — эпизоды очереди gaps-and-islands (начало/конец, ended_at null = стоит сейчас): возраст очереди на пине и в карточке, медиана очереди; питает прогноз очереди в карточке АЗС"
      ],
      "training": [
        "prediction_training — верифицированные прогнозы с error_minutes и features (вход измерений и тюнинга)"
      ],
      "notes": [
        "Региональные вьюхи: city-измерение ('south' + 9 городов боксами) через cross join lateral (values ...)",
        "Все вьюхи установлены с security_invoker = true — RLS применяется под маской запросившего, а не создателя вьюхи",
        "Вьюхи обходят лимит Supabase REST в 1000 строк: агрегаты считаем вьюхами, а не выгрузкой сырых строк"
      ]
    },
    "whitelists": {
      "events_event_type_check": ["fuel_disappeared", "fuel_restored", "fuel_available", "fuel_unavailable", "possible_delivery", "queue_high", "queue_low", "queue_appeared", "queue_gone"],
      "user_feedback_insert_rls": "feedback_type/fuel_type/queue_size только из списка чипсов, device_id обязателен",
      "rule": "Новый тип события = сначала расширить constraint, потом писать код"
    }
  },
  "done": [
    "Этапы 1–4: API GdeBenz, коллектор на Actions, история наблюдений, gap-детектор + фильтр противофазы",
    "Безопасность: service_role только в секретах; RLS везде; вьюхи security_invoker; белый список user_feedback; аноним = select",
    "Этап 5: предиктор v1 → v1.1 (пулинг own→brand→city, модель длительности дефицита, expected_restore_at, всё по Москве)",
    "Первый 🟢 fuel_restored (15.09, 12:11) и первые прогнозы (15.09, 21:31, brand-пул)",
    "Этап 6/D: верификатор v1 с прицелом target_date; сайт v2; пульс города; бот v2; бренды-логотипы; дайджест 08:00",
    "Тепловая матрица 4×24 + кардиограмма День/Неделя/Месяц + мультивыбор отметок + дайджест-панель + защищённая загрузка (try/catch per view) + честные пустые состояния + чипы «Сейчас · АИ-95 есть» (16.09)",
    "Обучающий контур v1.2 + верификатор v1.2 (величина ошибки) + предиктор v1.3 (k-NN) + блок «Почему такой прогноз» + collector v4 без комментариев (17.09)",
    "Предиктор v1.4 (гейт молчащих > 7 сут) + selling-нормировка очередей + «Куда ехать» с гейтом свежести + baseline-сравнение + TRAINING DATA + фильтр/сортировка + живая лента + режим UNKNOWN ночью + logTrainingData как функция (ночь 17→18.09)",
    "Волна 1: донор Кубань+Адыгея (~698 станций), stations.region, экономная запись, вьюхи city_events/city_daily_events с bbox (ночь 17→18.09)",
    "Карта спроса: MapLibre + OpenFreeMap positron, «чистая бумага», зоны/кластеры/пины с тап-полётом и разлётом пинов (18.09)",
    "Сайт v2.5 (ночь 18→19.09): геолокация и точка «я здесь»; зоны v2; палитра пинов по топливу + градиентные пины; таблички названий с логотипом бренда; вылетающие чипы; кольцо действий в стиле The Sims; пузырь быстрой отметки с подтверждением; метрики ① цены ② покрытие ③ возраст очереди; шапка панели списка; легенда карты в две строки",
    "Сайт v2.6 + предиктор v1.5 (19.09): панель «Наша точность прогнозов»; стабильность кластеров при панорамировании; «Куда ехать» с расстоянием; queue_age_min в features; последнее спокойное окно + совет «очередь HH:MM»",
    "Расширение на весь юг → сайт v2.7 (19.09 вечер): collector v5 heartbeat доноров 1ч; predictor с рамкой всего юга; 4 region-вьюхи с city-измерением; персональный город по геолокации (CITY_BOXES 9 городов); шапка «Рядом | Помощь» + супрафикс «юг»; Q&A-карточка «Помощь» + приветственное окно; хотфикс потерянной скобки scopeIds()",
    "Сайт v2.8 ч.1 (20.09): кнопка «Попробовать снова»; народный консенсус на карте (1 голос = чип «· народ», 2 = окраска пина, 3 = «народ подтвердил», конфликт = оранжевый пин с «?»; приоритет источника; консенсус НЕ пишется в observations); прогноз очереди в карточке (медианное окно ±σ, своих событий <3 → город, why-pop)",
    "Сайт v2.8 ч.2 + collector v5.1 + predictor v1.7 (21.09): sheet-scroll (лечение чёрных углов мобильного Chrome); пауза пульса зон под шторкой и на других панелях; PWA (manifest.json + sw.js, установка на домашний экран, standalone); collector v5.1 upstream-отказоустойчивость (мягкий выход 0, троттлинг алерта ≤1ч через bot_meta.upstream_alert, джиттер ≤45с); predictor v1.7 (день недели в k-NN)",
    "Офлайн-контур + калибровка (22.09): офлайн-режим сайта (снимок kbCache только успешной загрузкой, плашка «Нет связи — показываем данные на ЧЧ:ММ…», отметки без сети в fbQueue с автоотправкой, loadPulse-catch не затирает тепло/события снимка через if (!offlineMode)); гуманизация текстов ошибки; calibrator v1→v3 + calibrate.yml + cron 02:25 MSK (починка ошибки выжившего: знаменатель — все закрытые окна; свежесть 14/30 дней; ворота ≥30 строк / ≥10 SUCCESS / разброс ≥15 п.п.; PAVA; самоудаление таблицы); predictor v1.8 (применение таблицы, conf_raw, model_version v1.2); sw.js: кэш только ok-ответов, CACHE_VERSION v3",
    "Сайт v2.9 (22.09 поздно): минимальная аналитика — таблица site_hits (анонимный session_id, referrer, utm_source/medium/campaign, city_key, offline, panel; RLS: anon только insert), события pageview / panel_view / open_station_card / click_where_to_go / feedback_submit, отправка пачками (5 или 4 сек) + fetch keepalive при скрытии вкладки, отказы тихие (.catch) и не ломают сайт; переупаковка панели точности (главный чип — медианная ошибка; чип «в N раз точнее простого»; попадания счётом «X из Y» вместо процента; строка «из проверенных измерено N, ещё M ждут факта возврата — в ошибку они не входят»); индикатор свежести данных (порог 6ч — тот же, что у советника «Куда ехать»; светофор ≥80% / ≥50% / иначе; цветная строка «Данные: N из M свежие» в подзаголовке панели 1 + чип в плашке карты с why-pop 'fresh'); блок «улучшение модели · ошибка по неделям» (медианы |error| по ISO-неделям verified_at, мы vs простой способ, вывод «было → стало» только при ≥3 измерениях в крайних неделях, иначе честная заглушка); CACHE_VERSION поднимался на каждую заплатку"
  ],
  "todo": [
    "Shadow-сравнение версий модели по model_version (прод = лучшая по медиане |error|)",
    "Волна 2: Ростов/Ставрополь (~1575 станций) — после замера выхода эпизодов с Кубани за неделю",
    "Волна 3: Москва/Новосибирск — доноры режима NORMAL под калибровку",
    "city_queue_episodes → region-измерение (возраст очереди для персонального города, не только домашнего бокса)",
    "Очереди этап 2: народные события queue_high/queue_appeared в почасовом q; модельная оценка очереди в дефицит с явной подписью «оценка»",
    "Поправка смещения ETA (bias +101 мин по TRAINING DATA) — после накопления измеренных строк вне дефицита",
    "Метрика ④ сходимость народных отметок с источником и метрика ⑤ цензурирование в дайджесте — заморожены решением продукта",
    "MAU-дашборд (удержание / источники прихода / воронка реклама→сайт→карта→«Куда ехать») поверх site_hits — сбор уже работает (сайт v2.9), дашборд и реклама на kogdabenz.ru — впереди",
    "TWA (Bubblewrap) — APK без бейджа Chrome и вход в Google Play: после подъёма kogdabenz.ru с .well-known/assetlinks.json",
    "Этап 7/F: модель v2 (триггер очереди с лагом, загруженность, биржевой фон)",
    "«Тихие часы» per-АЗС (закрыть обещание hero: «и меньше очередей»)",
    "Кнопка «поделиться прогнозом» (Web Share API)",
    "Черновики → legacy/; резервная копия базы раз в неделю",
    "Публичный Telegram-канал с утренним дайджестом для города",
    "Приоритет №4 перед рекламой: неделя спокойного наблюдения без новых фич (стабильность сайта, sw-кэш, офлайн, отметки, карта на среднем телефоне)"
  ],
  "rules": [
    "Запросы к GdeBenz — только с BROWSER_HEADERS и повторами",
    "Рабочий код не переписываем с нуля: возможности дописываются шагами",
    "Новые скрипты — один файл, чистый fetch, без npm-зависимостей",
    "Всё время — явно по Москве (UTC+3): Actions и база живут в UTC",
    "Факт и прогноз в интерфейсе разделены всегда (ТЗ §15)",
    "Точность показываем только при ≥10 проверенных прогнозах (ТЗ §12)",
    "Прогноз прицеливается на дату (target_date); верификатор трогает только вчерашние цели",
    "Официальные городские сводки не используем; Яндекс/2GIS/банки не парсим (ToS, антибот) — данные берём легально: GdeBenz API + своя толпа",
    "Очередь — опережающий сигнал: появилась раньше отметок топлива",
    "Фильтр противофазы: дизель и бензин в противофазу за один снимок = артефакт состава отметок, дизельный флаг выбрасываем",
    "Новый тип события → сначала белый список constraint, потом код",
    "Помним про лимит Supabase REST (1000 строк): агрегаты считаем вьюхами, а не выгрузкой сырых строк",
    "Вьюхи — всегда security_invoker = true, чтобы RLS работал сквозь них",
    "Народные отметки с структурой → в базе только значения из белого списка (RLS policy)",
    "Серверлесс где возможно: бот без сервера, на cron-job.org + Supabase meta-таблица",
    "Отказоустойчивость сайта: каждый внешний запрос к вьюхе в своём try/catch; отрисовка блоков не падает от одной упавшей вьюхи",
    "Пустые состояния видимы: нет данных → серая плашка с объяснением, не «пусто на белом» и не суррогат ради плотности",
    "Мультивыбор в UI: несколько фактов одновременно — отправляем массивом",
    "Обучение только на верифицированных исходах: верификатор — единственные ворота обучения",
    "CREATE OR REPLACE VIEW: новые колонки только дописываются в конец — иначе ошибка 42P16",
    "Изменения модели — аддитивно с фолбэком на малых данных: k-NN при поддержке ≥8 эпизодов, иначе старая медиана",
    "Измерение раньше усложнения: сначала error_minutes/baseline_error_minutes и TRAINING DATA, потом новые модели; тяжёлое ML — не раньше сотен верифицированных прогнозов",
    "Экономная запись донор-наблюдений: для region not null — только смены/heartbeat 1ч/первая встреча",
    "Городские сводки домашнего бокса — из city-вьюх; региональные/персональные — из region-вьюх с фильтром city",
    "Карта спроса: чужие слои скрываем рантаймом после load; кластеры считаем на мировых координатах по всем станциям, рисуем только центры в кадре; тап по маркерам слушаем в capture-фазе document",
    "Сайт персонален по геолокации: список, поиск, чипы, плашка, «Куда ехать», теглайн и кардиограмма по умолчанию = город пользователя (CITY_BOXES → cityStationList()); весь юг — через карту и тумблер скоупа",
    "Панели сайта не скроллятся вниз (кроме списка «АЗС города»): новые сущности — только оверлеи или уплотнение существующих блоков",
    "Быстрые действия на карте работают без открытия карточки; любая отправка народной отметки обязана иметь видимое подтверждение",
    "Палитра едина: один цвет одного смысла во всех сущностях карты (пины, полоса кластера, вылетающие чипы, легенда)",
    "Прозрачности и толщины GL-слоёв задаём обычными числами из JS (рампа по зуму + пульс): составные выражения с interpolate(zoom) MapLibre применяет непредсказуемо",
    "Анимации маркеров не перебивают центрирующий transform: отдельные keyframes с translate внутри",
    "Советник «Куда ехать» учитывает расстояние (штраф min(25, км × 1.5)) и советует только станции со свежими данными ≤6 ч и ненулевым топливом",
    "Приветственное окно — один раз на устройство (localStorage.seenHello); карточка «Помощь» — простым языком, без технических терминов",
    "После серии ручных правок на мобильном — проверять целостность файла (обрыв скрипта или потерянная скобка = сайт в заглушках без ошибок в консоли элементов)",
    "Любая правка index.html или manifest.json → поднять CACHE_VERSION в sw.js: статика отдаётся cache-first, иначе устройства останутся на старой копии сайта (кейс 22.09)",
    "Service worker кэширует только ok-ответы: 503/ошибки не должны консервироваться и возвращаться как «данные»",
    "Народный консенсус: приоритет источника (народ красит пин только когда источник молчит обо всех топливах); голоса считаем по разным device_id за 60 минут; 2 голоса — окраска, 3 — «народ подтвердил», одновременные «есть»/«нет» — оранжевый пин с «?» и чип «разные мнения»; консенсус не пишется в observations (мягкий слой карты)",
    "Шторка: внешняя .sheet — скругления и overflow:hidden без прокрутки; контент скроллится во внутренней .sheet-scroll (лечение артефакта чёрных углов мобильного Chrome)",
    "Пульс зон карты останавливается при открытой шторке/приветственном окне и на панелях кроме карты: непрерывная перерисовка WebGL под подложкой даёт мерцание по краям экрана",
    "Ворота калибровки: ≥30 верифицированных строк в окне свежести (14 дней, фолбэк 30), ≥10 SUCCESS, разброс бинов ≥15 п.п.; ворота не пройдены → таблица самоудаляется, предиктор на сырой уверенности; знаменатель калибровки — все закрытые окна (MISS без возврата = честный промах, а не цензура)",
    "Офлайн: снимок пишется только успешной загрузкой и не перезаписывается офлайн-данными; отметки без сети → fbQueue с автоотправкой; ошибка загрузки без снимка — дружелюбная плашка с кнопкой «Попробовать снова», не технический текст",
    "Коллектор: недоступность источника (429/503/сеть) = мягкий выход 0 + троттлинг алерта ≤1 раза в час (bot_meta.upstream_alert); exit 1 и красный workflow — только собственные баги коллектора",
    "Аналитика анонимна: без персональных данных; session_id случайный в localStorage; геопозиция только та, что продукт уже использует (city_key); site_hits для anon — insert-only, чтение только владельцем; отказ отправки не должен ломать сайт (.catch)",
    "Порог свежести данных един по продукту: 6 часов (советник «Куда ехать» и индикатор свежести); светофор: ≥80% свежих — зелёный, ≥50% — жёлтый, иначе красный",
    "Упаковка точности: сначала полезный смысл (медианная ошибка, сравнение с простым способом), затем сырой счёт попаданий «X из Y», затем объяснение, почему измеренных мало; пугающий процент без контекста главным числом не ставим",
    "Улучшение модели показываем медианами |error| по ISO-неделям verified_at против простого способа; вывод «было → стало» только при ≥3 измерениях в крайних неделях — иначе это шум, а не тренд"
  ],
  "run_and_check": {
    "manual": "Actions → любой workflow → Run workflow",
    "norms": {
      "collect": "Станций в ответе: ~137 + Донор kuban: станций ~698 + Наблюдений записано: дом 137 + доноры N; при недоступности источника — ⚠️ Источник недоступен: … и зелёный workflow (мягкий выход 0), алерт в Telegram не чаще раза в час",
      "predict": "Контекст региона: … режим …; Эпизодов дефицита для k-NN: N; 4.9) Читаю таблицу калибровки уверенности… + одно из: Таблицы калибровки ещё нет — сырая уверенность / Калибровка применится: таблица от … по N проверенным прогнозам; далее Создано: N, обновлено: N, пропущено: N, молчащих станций (>72ч): N",
      "verify": "Ночью построчные ✅ SUCCESS / ❌ MISS с величиной ошибки и TRAINING DATA (пустой прогон тоже печатает)",
      "calibrate": "=== КАЛИБРАТОР v3 === + Окно 14 дн: строк N, попаданий M; ворота не пройдены — Мало попаданий (M < 10)… Таблица удалена…; пройдены — строки бинов сырая 0.46 → факт 12% (n 40) и ✅ Таблица калибровки записана (…)",
      "probe": "=== РАЗВЕДКА ПОКРЫТИЯ GdeBenz === + по строке на каждую рамку",
      "digest": "Одно сообщение в Telegram вида 🌅 КогдаБенз · дайджест за сутки…",
      "bot": "/start → клавиатура кнопок → 📊 Сводка города → отчёт о дефицитах и очередях",
      "site": "Список и чипы показывают город пользователя; тумблер «город / весь юг» перезагружает кардиограмму/тепло/чипы; «Помощь» открывает Q&A-шторку; приветственное окно один раз; «Куда ехать» — станция города с километрами; карта показывает весь юг кластерами и пинами; народный консенсус красит пины молчащих станций по правилам 2/3/конфликт; в карточке — полоса «Очередь вероятна: …»; PWA ставится на домашний экран и открывается standalone; при падении загрузки со снимком — оранжевая плашка «Нет связи — показываем данные на ЧЧ:ММ ДД.ММ», отметки без сети дают подтверждение «сохранена, уйдёт автоматически»; v2.9: подзаголовок панели 1 начинается цветной строкой «Данные: N из M свежие» (сверка с SQL по station_last_obs), в плашке карты чип свежести с why-pop, панель точности в порядке: медианная ошибка → в N раз точнее → попали точно в окно X из Y → проверено ночами, ниже блок «улучшение модели · ошибка по неделям»; аналитика пишет в site_hits (проверка: select event, count(*) from site_hits group by 1)"
    },
    "sql_self_check": [
      "select event_type, count(*) from events group by event_type;",
      "select count(*) filter (where timestamp > now() - interval '1 hour') as за_час, count(*) as всего from observations;",
      "select region, count(*) from stations group by 1;",
      "select target_date, result, count(*) from predictions group by 1,2 order by 1 desc;",
      "select key, value from bot_meta;",
      "select value from bot_meta where key = 'calibration_table';",
      "select count(*) from prediction_training;",
      "select count(*) filter (where error_minutes is not null) as с_ошибкой, count(*) filter (where baseline_error_minutes is not null) as с_baseline from predictions where is_verified;",
      "select city, count(*) from region_hourly group by 1 order by 2 desc;",
      "select city, count(*) from region_fuel_hourly group by 1 order by 2 desc;",
      "select count(*) from stations where lat between 44.60 and 44.85 and lon between 37.55 and 38.00;",
      "select count(*) from city_price_hourly where hour > now() - interval '7 days';",
      "select count(*) from city_queue_episodes where started_at > now() - interval '3 days';",
      "select event, count(*) from site_hits group by 1 order by 2 desc;",
      "select date_trunc('day', created_at) as day, count(distinct session_id) as сессии, count(*) as хиты from site_hits group by 1 order by 1;",
      "select count(*) filter (where o.data_freshness_minutes <= 360) as свежие, count(*) as всего from station_last_obs o join stations s on s.id = o.station_id where s.lat between 44.60 and 44.85 and s.lon between 37.55 and 38.00;"
    ],
    "timezone_note": "Table Editor показывает время в UTC; новороссийское = +3"
  },
  "security": [
    "Секреты GitHub: SUPABASE_URL, SUPABASE_KEY (service_role), TELEGRAM_BOT_TOKEN, TELEGRAM_CHAT_ID",
    "В клиентский код (index.html) — только anon-ключ",
    "PAT для cron-job.org: fine-grained, Actions Read and write, один репозиторий; ротация при любой утечке",
    "Антиспам народных отметок: ≤3 с устройства за 30 минут (localStorage) + RLS-белый список типов/топлива/размеров очереди",
    "Вьюхи security_invoker = RLS применяется под маской запросившего",
    "bot_meta доступна только service_role — состояние бота, троттлинг алертов и таблица калибровки не торчат наружу",
    "probe.js не требует секретов: только публичный GdeBenz с паспортом браузера",
    "Аналитика site_hits: без персональных данных, геопозиция только с явного согласия (город уже используется продуктом), session id случайный; anon — только insert",
    "PWA: service worker кэширует только ok-ответы; инвалидация кэша — только подъёмом CACHE_VERSION"
  ],
  "troubleshooting": [
    { "symptom": "GdeBenz ответил 502/403", "fix": "Защита от ботов или чих: BROWSER_HEADERS + 3 повтора уже в коде; план Б — cookie в секреты" },
    { "symptom": "«сборщик упал!» спамит каждые 10 минут во время чужой DDoS на источник", "fix": "upstream-ошибки (429/503/сеть) = мягкий выход 0 + алерт ≤1 раза в час (bot_meta.upstream_alert); красный workflow — только собственные баги" },
    { "symptom": "violates check constraint events_event_type_check", "fix": "Новый тип события не в белом списке → расширить constraint" },
    { "symptom": "Dispatch от cron-job: 404 Not Found", "fix": "Workflow-файл лежит не в .github/workflows/ (классика мобильного GitHub: папка-двойник)" },
    { "symptom": "ReferenceError: X is not defined после заплатки", "fix": "Объявление переменной внутри блока if — вынести на уровень использования" },
    { "symptom": "SyntaxError: Unexpected end of input после серии заплаток", "fix": "Разбаланс фигурных скобок при ручной вставке кусков — заменить файл целиком на выверенный" },
    { "symptom": "Сайт стоит в заглушках без ошибок в элементах", "fix": "Скрипт оборван или функция не закрыта (кейс: потеряна } у scopeIds()); проверить хвост файла, восстановить закрытие" },
    { "symptom": "Сайт показывает старую версию после коммита", "fix": "Service worker cache-first: поднять CACHE_VERSION в sw.js; без этого устройства не получат новый index.html" },
    { "symptom": "503 «консервируется» и отдаётся без сети", "fix": "sw.js кэшировал не-ok ответы; теперь в кэш идёт только response.ok" },
    { "symptom": "404 «There isn't a GitHub Pages site here»", "fix": "Pages не опубликован: Settings→Pages (Source: branch main + / (root)), видимость репозитория Public, index.html в корне на месте" },
    { "symptom": "Чёрные «клинья» по углам шторки на мобильном", "fix": "Скругления на скроллящемся слое (GPU-артефакт): скролл вынесен во внутренний .sheet-scroll, внешняя .sheet с overflow:hidden" },
    { "symptom": "Мерцание по краям экрана под шторкой", "fix": "Пульс зон перерисовывал WebGL 15 раз/сек под blur-подложкой: пульс останавливается при открытой шторке и вне панели карты" },
    { "symptom": "Офлайн: тепло серое, «Пик спроса —» при живом снимке", "fix": "loadPulse-catch затирал fuelHourly/dailyEvents из снимка: охрана if (!offlineMode); ранний saveCache() из start() убран" },
    { "symptom": "Уверенность скакнула в 95%, затем в 5%", "fix": "Калибровка: ошибка выжившего (знаменатель только строки с возвратом) → знаменатель все закрытые окна; плоская таблица дефицита (0–3%) → ворота разброса ≥15 п.п. + самоудаление таблицы" },
    { "symptom": "«Отслеживаем 0» / «топливо видно: 0 из 0»", "fix": "localIdsGlobal посчитан до присвоения allStations в start() — считать как stations.map(s => s.id) после загрузки" },
    { "symptom": "Чипы/плашка показывают южные цифры вместо городских", "fix": "Циклы renderHero/renderNow/updateMapPlate/toggleCovPop идут по localIdsGlobal; должны идти по citySet = new Set(cityStationList().map(s => s.id))" },
    { "symptom": "Кардиограмма пустая в обоих скоупах", "fix": "Вьюха region_hourly не создана или city-фильтр не матчится: проверить select city, count(*) from region_hourly group by 1" },
    { "symptom": "Строка «Очереди» серая, линия спроса нулевая после патча", "fix": "Новая колонка вьюхи (selling/knownany) не добавлена в явный select= запроса сайта — дописать" },
    { "symptom": "42P16 cannot change name of view column при пересоздании вьюхи", "fix": "В CREATE OR REPLACE VIEW новые колонки дописываются только в конец" },
    { "symptom": "Очереди «всё зелёное» при живых пробках", "fix": "Знаменатель «все станции» размывает очередь дефицита; очереди нормируются на selling" },
    { "symptom": "Кардиограмма: шкала до 800%", "fix": "Норма в дефицит: очереди у станций без топлива дают queue_marks > selling; линейка динамическая" },
    { "symptom": "«Куда ехать» ведёт на АЗС с «нет данных»", "fix": "Гейт data_freshness_minutes ≤ 360 + приоритет «топливо есть сейчас» + ненулевой статус топлива" },
    { "symptom": "«Куда ехать» ведёт на дальнюю АЗС", "fix": "Штраф расстояния min(25, км × 1.5) + городской скоуп советника" },
    { "symptom": "«Молчащих станций: 28 из 29» после гейта", "fix": "Гейт только по data_freshness_minutes > 7 сут (возраст строки непригоден в дефицит)" },
    { "symptom": "prediction_training = 0 сразу после миграции", "fix": "Норма до первого verify новым кодом (02:10 MSK)" },
    { "symptom": "Цензурировано: 100% верифицированных / панель точности 0/N", "fix": "В дни глубокого дефицита возвраты редки; MISS без факта = error_minutes = null; это сигнал, не поломка; панель оживёт при 3+ измеренных" },
    { "symptom": "regime:UNKNOWN в TRAINING DATA", "fix": "Норма для ночных часов с known95 = 0" },
    { "symptom": "TRAINING DATA не печатается в пустом прогоне verify", "fix": "Оставить только вызов await logTrainingData() в двух местах" },
    { "symptom": "«Доноры 0» между часовыми границами", "fix": "Норма при heartbeat 1ч: доноры пишутся на часовой границе и при сменах" },
    { "symptom": "Приветственное окно не показывается", "fix": "localStorage.seenHello уже установлен; для теста удалить ключ" },
    { "symptom": "Бейдж Chrome на иконке PWA", "fix": "Системная метка веб-приложения; убирается только TWA/APK с собственным доменом и assetlinks.json" },
    { "symptom": "Легенда кардиограммы уезжает под кнопки", "fix": "Уплотнение панели 1 + overflow:hidden на .mini-graph; при нехватке высоты масштабируется svg" },
    { "symptom": "Тепловая матрица серая / кардиограмма пустая (домашний скоуп)", "fix": "Вьюха region_fuel_hourly не создана или фильтр city=novorossiysk не матчится с боксом" },
    { "symptom": "Кардиограмма Неделя/Месяц: b.getFullYear is not a function", "fix": "sameDay(nowD, b) получил объект-бакет вместо даты: правильно sameDay(nowD, b.t)" },
    { "symptom": "Карта не загружается / белое пятно", "fix": "Тайлы OpenFreeMap — внешний хост, из РФ может идти медленно; заменить URL стиля одной строкой в ensureMap" },
    { "symptom": "Тап по квадрату кластера не летит", "fix": "Document-детектор в capture-фазе с preventDefault() на pointerdown по маркеру и задержкой полёта ~120 мс после pointerup" },
    { "symptom": "Пины плавают во время жестов панорамирования", "fix": "CSS .maplibregl-marker с transition:none !important; animation:none !important; will-change:transform — не перебивать" },
    { "symptom": "Кластеры прыгают при панорамировании и меняют цифру", "fix": "Ячейки считать по всем станциям, рисовать только центры в кадре" },
    { "symptom": "Чипы кольца «съезжали» при появлении", "fix": "У кольца свои keyframes mPopRing с translate внутри" },
    { "symptom": "Синие чипы кольца не реагировали на тап", "fix": "Чипы владеют жестом сами (stopPropagation pointerdown/pointerup), действие на click" },
    { "symptom": "Плашка/полоса рвёт слова при переносе", "fix": "Неразрывные сегменты .ps-wrap / .ps: перенос только между смысловыми блоками" },
    { "symptom": "Быстрая отметка отправлялась «в тишину»", "fix": "Подтверждение обязательно: пузырь показывает «Спасибо 🙏» (или «нет связи — сохранена») и сам закрывается" },
    { "symptom": "Чип покрытия всегда «0 из N»", "fix": "knownany не добавлен в явный select= запроса к region_fuel_hourly — дописать" },
    { "symptom": "Многоточие в ячейках карточки", "fix": "text-overflow: ellipsis режет длинный хвост; цена и дельта разнесены на две строки" },
    { "symptom": "site_hits пустая после деплоя, хотя клики были", "fix": "RLS: политика для anon — insert-only, select нет (это норма, чтение только владельцем); проверять из SQL-консоли Supabase; также убедиться, что поднят CACHE_VERSION и телефон получил новый index.html" },
    { "symptom": "Индикатор свежести всегда красный", "fix": "Не баг: источник молчит (data_freshness_minutes null или >6ч); в глубоком дефиците это честная картина — пины несут народные отметки; сверка: SQL по station_last_obs с порогом 360" },
    { "symptom": "Блок «улучшение модели» показывает заглушку или одну неделю", "fix": "Норма: недель с измерениями <2 либо в крайней неделе <3 измерений — вывод «было → стало» скрыт, чтобы не показывать шум как тренд" }
  ],
  "reference_sql": [
    "alter table public.events drop constraint events_event_type_check;",
    "alter table public.events add constraint events_event_type_check check (event_type in ('fuel_disappeared','fuel_restored','fuel_available','fuel_unavailable','possible_delivery','queue_high','queue_low','queue_appeared','queue_gone'));",
    "alter table public.predictions add column if not exists target_date date;",
    "update public.predictions set target_date = (created_at at time zone 'Europe/Moscow')::date where target_date is null;",
    "alter table public.predictions add column if not exists actual_restore_at timestamptz;",
    "alter table public.predictions add column if not exists error_minutes int;",
    "alter table public.predictions add column if not exists features jsonb;",
    "alter table public.predictions add column if not exists model_version text default 'v1.1';",
    "alter table public.predictions add column if not exists baseline_restore_at timestamptz;",
    "alter table public.predictions add column if not exists baseline_error_minutes int;",
    "create index if not exists idx_predictions_verified_error on public.predictions (is_verified, model_version) where is_verified = true and error_minutes is not null;",
    "alter table public.stations add column if not exists region text;",
    "create index if not exists idx_stations_region on public.stations (region);",
    "create table if not exists public.site_hits (id bigint generated always as identity primary key, created_at timestamptz not null default now(), session_id text not null, event text not null, referrer text, utm_source text, utm_medium text, utm_campaign text, city_key text, offline boolean not null default false, panel int);",
    "alter table public.site_hits enable row level security;",
    "create policy site_hits_insert on public.site_hits for insert to anon, authenticated with check (session_id is not null and event is not null);",
    "create or replace view public.station_counts as select station_id, count(*) as total from public.observations group by station_id;",
    "alter view public.station_counts set (security_invoker = true);",
    "create or replace view public.station_hourly as select station_id, date_trunc('hour', timestamp) as hour, count(*) as total, count(*) filter (where fuel_95_status = true) as available_95 from public.observations group by 1, 2;",
    "alter view public.station_hourly set (security_invoker = true);",
    "create or replace view public.city_hourly as select date_trunc('hour', o.timestamp) as hour, count(*) as marks, count(*) filter (where o.queue_level = 'high') as queue_marks, count(*) filter (where o.fuel_95_status = true) as avail95, count(*) filter (where o.fuel_95_status is not null) as known95, count(*) filter (where o.fuel_92_status = true or o.fuel_95_status = true or o.diesel_status = true) as selling from public.observations o join public.stations s on s.id = o.station_id where s.lat >= 44.60 and s.lat <= 44.85 and s.lon >= 37.55 and s.lon <= 38.05 group by 1;",
    "alter view public.city_hourly set (security_invoker = true);",
    "create or replace view public.city_fuel_hourly as select date_trunc('hour', o.timestamp) as hour, count(*) filter (where o.fuel_92_status = true) as a92, count(*) filter (where o.fuel_92_status is not null) as k92, count(*) filter (where o.fuel_95_status = true) as a95, count(*) filter (where o.fuel_95_status is not null) as k95, count(*) filter (where o.diesel_status = true) as adt, count(*) filter (where o.diesel_status is not null) as kdt, count(*) filter (where o.queue_level = 'high') as q, count(*) as marks, count(*) filter (where o.fuel_92_status = true or o.fuel_95_status = true or o.diesel_status = true) as selling, count(*) filter (where o.fuel_92_status is not null or o.fuel_95_status is not null or o.diesel_status is not null) as knownany from public.observations o join public.stations s on s.id = o.station_id where s.lat >= 44.60 and s.lat <= 44.85 and s.lon >= 37.55 and s.lon <= 38.05 group by 1;",
    "alter view public.city_fuel_hourly set (security_invoker = true);",
    "create or replace view public.city_events as select e.station_id, e.event_type, e.detected_at from public.events e join public.stations s on s.id = e.station_id where s.lat >= 44.60 and s.lat <= 44.85 and s.lon >= 37.55 and s.lon <= 38.05;",
    "alter view public.city_events set (security_invoker = true);",
    "create or replace view public.city_daily_events as select date_trunc('day', e.detected_at) as day, count(*) filter (where e.event_type = 'fuel_disappeared') as dis, count(*) filter (where e.event_type = 'fuel_restored') as res from public.events e join public.stations s on s.id = e.station_id where s.lat >= 44.60 and s.lat <= 44.85 and s.lon >= 37.55 and s.lon <= 38.05 group by 1;",
    "alter view public.city_daily_events set (security_invoker = true);",
    "create or replace view public.city_price_hourly as select date_trunc('hour', o.timestamp) as hour, percentile_cont(0.5) within group (order by o.price_92) as p92, percentile_cont(0.5) within group (order by o.price_95) as p95, percentile_cont(0.5) within group (order by o.price_diesel) as pdt, min(o.price_95) as min95, max(o.price_95) as max95, count(*) filter (where o.price_95 is not null) as n95, count(*) as marks from public.observations o join public.stations s on s.id = o.station_id where s.lat >= 44.60 and s.lat <= 44.85 and s.lon >= 37.55 and s.lon <= 38.05 group by 1;",
    "alter view public.city_price_hourly set (security_invoker = true);",
    "create or replace view public.station_last_price as select distinct on (station_id) station_id, price_92, price_95, price_diesel, timestamp from public.observations where price_92 is not null or price_95 is not null or price_diesel is not null order by station_id, timestamp desc;",
    "alter view public.station_last_price set (security_invoker = true);",
    "create or replace view public.city_queue_episodes as with ev as (select e.station_id, case when e.event_type in ('queue_high','queue_appeared') then 'pos' else 'neg' end as kind, e.detected_at from public.events e join public.stations s on s.id = e.station_id where s.lat >= 44.60 and s.lat <= 44.85 and s.lon >= 37.55 and s.lon <= 38.05 and e.event_type in ('queue_high','queue_appeared','queue_low','queue_gone')), numbered as (select station_id, kind, detected_at, sum(case when kind = 'pos' then 1 else 0 end) over (partition by station_id order by detected_at rows between unbounded preceding and current row) as grp from ev), islands as (select station_id, grp, min(detected_at) filter (where kind = 'pos') as started_at, min(detected_at) filter (where kind = 'neg') as ended_at from numbered group by station_id, grp) select station_id, started_at, ended_at from islands where started_at is not null;",
    "alter view public.city_queue_episodes set (security_invoker = true);",
    "create or replace view public.region_hourly as select g.city, date_trunc('hour', o.timestamp) as hour, count(*) as marks, count(*) filter (where o.queue_level = 'high') as queue_marks, count(*) filter (where o.fuel_95_status = true) as avail95, count(*) filter (where o.fuel_95_status is not null) as known95, count(*) filter (where o.fuel_92_status = true or o.fuel_95_status = true or o.diesel_status = true) as selling from public.observations o join public.stations s on s.id = o.station_id cross join lateral (values ('south'), (case when s.lat between 44.60 and 44.85 and s.lon between 37.55 and 38.00 then 'novorossiysk' when s.lat between 44.45 and 44.68 and s.lon between 37.95 and 38.25 then 'gelendzhik' when s.lat between 44.80 and 45.10 and s.lon between 37.20 and 37.60 then 'anapa' when s.lat between 44.85 and 45.05 and s.lon between 37.85 and 38.15 then 'krymsk' when s.lat between 44.90 and 45.15 and s.lon between 38.80 and 39.15 then 'krasnodar' when s.lat between 43.35 and 43.75 and s.lon between 39.60 and 40.10 then 'sochi' when s.lat between 44.00 and 44.20 and s.lon between 39.00 and 39.25 then 'tuapse' when s.lat between 44.90 and 45.10 and s.lon between 40.90 and 41.20 then 'armavir' when s.lat between 44.55 and 44.75 and s.lon between 40.00 and 40.30 then 'maykop' else null end)) as g(city) where g.city is not null group by 1, 2;",
    "alter view public.region_hourly set (security_invoker = true);",
    "create or replace view public.region_fuel_hourly as select g.city, date_trunc('hour', o.timestamp) as hour, count(*) filter (where o.fuel_92_status = true) as a92, count(*) filter (where o.fuel_92_status is not null) as k92, count(*) filter (where o.fuel_95_status = true) as a95, count(*) filter (where o.fuel_95_status is not null) as k95, count(*) filter (where o.diesel_status = true) as adt, count(*) filter (where o.diesel_status is not null) as kdt, count(*) filter (where o.queue_level = 'high') as q, count(*) as marks, count(*) filter (where o.fuel_92_status = true or o.fuel_95_status = true or o.diesel_status = true) as selling, count(*) filter (where o.fuel_92_status is not null or o.fuel_95_status is not null or o.diesel_status is not null) as knownany from public.observations o join public.stations s on s.id = o.station_id cross join lateral (values ('south'), (case when s.lat between 44.60 and 44.85 and s.lon between 37.55 and 38.00 then 'novorossiysk' when s.lat between 44.45 and 44.68 and s.lon between 37.95 and 38.25 then 'gelendzhik' when s.lat between 44.80 and 45.10 and s.lon between 37.20 and 37.60 then 'anapa' when s.lat between 44.85 and 45.05 and s.lon between 37.85 and 38.15 then 'krymsk' when s.lat between 44.90 and 45.15 and s.lon between 38.80 and 39.15 then 'krasnodar' when s.lat between 43.35 and 43.75 and s.lon between 39.60 and 40.10 then 'sochi' when s.lat between 44.00 and 44.20 and s.lon between 39.00 and 39.25 then 'tuapse' when s.lat between 44.90 and 45.10 and s.lon between 40.90 and 41.20 then 'armavir' when s.lat between 44.55 and 44.75 and s.lon between 40.00 and 40.30 then 'maykop' else null end)) as g(city) where g.city is not null group by 1, 2;",
    "alter view public.region_fuel_hourly set (security_invoker = true);",
    "create or replace view public.region_events as select g.city, e.station_id, e.event_type, e.detected_at from public.events e join public.stations s on s.id = e.station_id cross join lateral (values ('south'), (case when s.lat between 44.60 and 44.85 and s.lon between 37.55 and 38.00 then 'novorossiysk' when s.lat between 44.45 and 44.68 and s.lon between 37.95 and 38.25 then 'gelendzhik' when s.lat between 44.80 and 45.10 and s.lon between 37.20 and 37.60 then 'anapa' when s.lat between 44.85 and 45.05 and s.lon between 37.85 and 38.15 then 'krymsk' when s.lat between 44.90 and 45.15 and s.lon between 38.80 and 39.15 then 'krasnodar' when s.lat between 43.35 and 43.75 and s.lon between 39.60 and 40.10 then 'sochi' when s.lat between 44.00 and 44.20 and s.lon between 39.00 and 39.25 then 'tuapse' when s.lat between 44.90 and 45.10 and s.lon between 40.90 and 41.20 then 'armavir' when s.lat between 44.55 and 44.75 and s.lon between 40.00 and 40.30 then 'maykop' else null end)) as g(city) where g.city is not null;",
    "alter view public.region_events set (security_invoker = true);",
    "create or replace view public.region_daily_events as select g.city, date_trunc('day', e.detected_at) as day, count(*) filter (where e.event_type = 'fuel_disappeared') as dis, count(*) filter (where e.event_type = 'fuel_restored') as res from public.events e join public.stations s on s.id = e.station_id cross join lateral (values ('south'), (case when s.lat between 44.60 and 44.85 and s.lon between 37.55 and 38.00 then 'novorossiysk' when s.lat between 44.45 and 44.68 and s.lon between 37.95 and 38.25 then 'gelendzhik' when s.lat between 44.80 and 45.10 and s.lon between 37.20 and 37.60 then 'anapa' when s.lat between 44.85 and 45.05 and s.lon between 37.85 and 38.15 then 'krymsk' when s.lat between 44.90 and 45.15 and s.lon between 38.80 and 39.15 then 'krasnodar' when s.lat between 43.35 and 43.75 and s.lon between 39.60 and 40.10 then 'sochi' when s.lat between 44.00 and 44.20 and s.lon between 39.00 and 39.25 then 'tuapse' when s.lat between 44.90 and 45.10 and s.lon between 40.90 and 41.20 then 'armavir' when s.lat between 44.55 and 44.75 and s.lon between 40.00 and 40.30 then 'maykop' else null end)) as g(city) where g.city is not null group by 1, 2;",
    "alter view public.region_daily_events set (security_invoker = true);",
    "create or replace view public.prediction_training as select id, station_id, fuel_type, from_time, to_time, confidence, prediction_source, based_on_observations, based_on_stations, target_date, created_at, actual_restore_at, error_minutes, features, baseline_error_minutes from public.predictions where is_verified = true and error_minutes is not null;",
    "alter view public.prediction_training set (security_invoker = true);",
    "do $$ declare r record; begin for r in select policyname from pg_policies where schemaname = 'public' and tablename = 'user_feedback' loop execute format('drop policy if exists %I on public.user_feedback', r.policyname); end loop; end $$;",
    "create policy user_feedback_select on public.user_feedback for select to anon, authenticated using (true);",
    "create policy user_feedback_insert on public.user_feedback for insert to anon, authenticated with check (device_id is not null and feedback_type in ('delivery','available','unavailable','queue','free') and (fuel_type is null or fuel_type in ('92','95','diesel','all')) and (queue_size is null or queue_size in ('small','medium','large','huge')));",
    "create table if not exists public.bot_meta (key text primary key, value text not null, updated_at timestamptz not null default now());",
    "alter table public.bot_meta enable row level security;"
  ],
  "data_sources": {
    "gdebenz": {
      "role": "Основной: отметки водителей, статусы, цены, очереди; текстовых комментариев в открытом API нет — шаг парсинга убран в collector v4, таблица comments спит",
      "home_frame": "Новороссийск+Геленджик+Анапа+Крымск (~137 АЗС) — полное наблюдение каждые 10 минут",
      "donor_frame_wave1": "Кубань+Адыгея (~698 АЗС, region='kuban') — экономная запись (смены/heartbeat 1ч); питает бренд-пулы, эпизоды k-NN и региональные сводки",
      "probe": "Разведка покрытия через probe.js (без записи в базу): источник жив на всю Россию (80–95% станций с ценами), но толпа в абсолюте тонкая; Москва/Новосибирск — в резерве волны 3 как доноры режима NORMAL",
      "reliability": "Внешние DDoS на gdebenz.ru (09.2026) не роняют проект — коллектор переживает их мягким выходом, сайт живёт на своих данных и снимке"
    },
    "own_crowd": "Структурированные кнопки на сайте (5 типов × топливо × размер очереди × комментарий) → user_feedback → события source='user_feedback'; на карте народные отметки работают как консенсус-фолбэк топлива (2/3 голоса, конфликт-пин) и как счётчик машин в очереди",
    "rejected": ["Официальные сводки (недостоверны)", "Парсинг Яндекса/2GIS/банков (ToS и антибот)", "Случайные Telegram-каналы (предупреждение Минэнерго)"],
    "model_v2_candidate": "Биржевые индексы СПбМТСБ/BenzUp как макропризнак дефицита (подключать при ≥300–400 верифицированных прогнозах)"
  },
  "chronology": [
    { "date": "14.09.2026", "event": "Первая версия сайта, Supabase-подключение" },
    { "date": "15.09.2026 утро", "event": "Первый рабочий коллектор, первые наблюдения" },
    { "date": "15.09.2026 день", "event": "Gap-детектор, фильтр противофазы, широкая рамка, телеграм-лента, дайджест, пульс города, бренды-логотипы" },
    { "date": "15.09.2026 12:11", "event": "Первый 🟢 fuel_restored (Газпром, АИ-95)" },
    { "date": "15.09.2026 21:31", "event": "Первые прогнозы (5 Роснефтей/дизель, brand-пул)" },
    { "date": "15.09.2026 вечер", "event": "Верификатор v1 + target_date, бот v2 с кнопками, security_invoker" },
    { "date": "16.09.2026 день", "event": "Тепловая матрица 4×24 + кардиограмма День/Неделя/Месяц + мультивыбор отметок + дайджест 6 метрик + защищённая загрузка + честные пустые состояния" },
    { "date": "17.09.2026 день", "event": "Обучающий контур v1.2; верификатор v1.2; предиктор v1.3 (k-NN); блок «Почему такой прогноз»; collector v4 без комментариев" },
    { "date": "17.09.2026 поздний вечер", "event": "Предиктор v1.4; selling-нормировка очередей; «Куда ехать» с гейтом свежести; baseline + TRAINING DATA с bias; фильтр/сортировка; живая лента; режим UNKNOWN ночью" },
    { "date": "ночь 17→18.09.2026", "event": "Волна 1 (донор Кубань+Адыгея): stations.region, экономная запись, лукбэк 24ч, вьюхи city_events/city_daily_events" },
    { "date": "18.09.2026", "event": "Карта спроса: MapLibre + OpenFreeMap positron, «чистая бумага», зоны/кластеры/пины с тап-полётом и разлётом пинов" },
    { "date": "ночь 18→19.09.2026", "event": "Сайт v2.5: геолокация; зоны v2; палитра и градиентные пины; таблички с логотипом; вылетающие чипы; кольцо действий; пузырь быстрой отметки; метрики ①②③" },
    { "date": "19.09.2026", "event": "Сайт v2.6 + предиктор v1.5: панель точности; стабильность кластеров; «Куда ехать» с расстоянием; queue_age_min; спокойное окно + совет «очередь HH:MM»" },
    { "date": "19.09.2026 вечер", "event": "Сайт v2.7 «юг»: collector heartbeat 1ч; predictor с рамкой всего юга; 4 region-вьюхи; персональный город по геолокации; «Помощь» + приветственное окно" },
    { "date": "20.09.2026", "event": "Сайт v2.8 ч.1: кнопка «Попробовать снова»; народный консенсус на карте (2 голоса красят, 3 подтверждают, конфликт — оранжевый пин с «?», приоритет источника, голоса по device_id за 60 мин); прогноз очереди в карточке (медианное окно ±σ, своих <3 → город, why-pop)" },
    { "date": "21.09.2026", "event": "Сайт v2.8 ч.2: sheet-scroll (лечение чёрных углов мобильного Chrome); пауза пульса зон под шторкой; PWA (manifest.json, sw.js, установка на домашний экран); collector v5.1 (upstream: мягкий выход 0, троттлинг алерта ≤1ч, джиттер); predictor v1.7 (день недели в k-NN); внешний DDoS на gdebenz.ru пережит без потери данных" },
    { "date": "22.09.2026", "event": "Офлайн-контур сайта (снимок kbCache + плашка «Нет связи — данные на ЧЧ:ММ» + очередь отметок fbQueue с автоотправкой); гуманизация ошибок загрузки; calibrator v1→v3 + calibrate.yml + cron 02:25 MSK (починка ошибки выжившего, свежесть 14/30 дней, ворота 30/10/15 п.п., PAVA, самоудаление таблицы); predictor v1.8 (применение калибровки, conf_raw, model_version v1.2); sw.js: кэш только ok-ответов, CACHE_VERSION v3" },
    { "date": "22.09.2026 поздно", "event": "Сайт v2.9: минимальная аналитика site_hits (анонимный session, utm/referrer, 5 событий, пачки + keepalive); переупаковка панели точности (медианная ошибка главным, «в N раз точнее простого», попадания счётом «X из Y», строка измерено/ждут факта); индикатор свежести данных (порог 6ч, светофор, строка в подзаголовке + чип плашки карты с why-pop); блок «улучшение модели · ошибка по неделям» (медианы |error| по ISO-неделям verified_at, мы vs простой способ, вывод «было → стало» при ≥3 измерениях в крайних неделях); CACHE_VERSION поднят на каждую заплатку" }
  ]
}